# Generated by generate-tenant-overlay.sh — do not edit manually
# Tenant: fakecorp  Environment: prod  Instance: fakecorp-prod
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fakecorp

resources:
  - ../../dotcms-base

images:
  - name: dotcms/dotcms
    newName: mirror.gcr.io/dotcms/dotcms
    newTag: latest

patches:
  # ── Rename resources (JSON patch) ──────────────────────────────────────────
  - target: {kind: Deployment, name: dotcms}
    patch: |
      - {op: replace, path: /metadata/name, value: fakecorp-prod}
  - target: {kind: Service, name: dotcms}
    patch: |
      - {op: replace, path: /metadata/name, value: fakecorp-prod}
  - target: {kind: Service, name: dotcms-hl}
    patch: |
      - {op: replace, path: /metadata/name, value: fakecorp-prod-hl}
  - target: {kind: HorizontalPodAutoscaler, name: dotcms}
    patch: |
      - {op: replace, path: /metadata/name, value: fakecorp-prod}
      - {op: replace, path: /spec/scaleTargetRef/name, value: fakecorp-prod}
  - target: {kind: PodDisruptionBudget, name: dotcms}
    patch: |
      - {op: replace, path: /metadata/name, value: fakecorp-prod}

  # ── Instance-specific Deployment config (strategic merge) ──────────────────
  - target: {kind: Deployment, name: dotcms}
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: dotcms
      spec:
        selector:
          matchLabels:
            instance: fakecorp-prod
        template:
          metadata:
            labels:
              instance: fakecorp-prod
              botcms.cloud/tenant: fakecorp
              botcms.cloud/instance: fakecorp-prod
          spec:
            containers:
              - name: dotcms
                resources:
                  requests:
                    cpu: "500m"
                    memory: "2Gi"
                  limits:
                    memory: "3Gi"
                env:
                  - name: DOT_DOTCMS_CLUSTER_ID
                    value: fakecorp-prod
                  - name: DB_BASE_URL
                    value: "jdbc:postgresql://postgres-rw.postgres.svc.cluster.local:5432/fakecorp-prod"
                  - name: DB_USERNAME
                    valueFrom:
                      secretKeyRef:
                        name: fakecorp-prod-postgres
                        key: username
                  - name: DB_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: fakecorp-prod-postgres
                        key: password
                  - name: DOT_ES_AUTH_BASIC_USER
                    valueFrom:
                      secretKeyRef:
                        name: fakecorp-prod-os-creds
                        key: username
                  - name: DOT_ES_AUTH_BASIC_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: fakecorp-prod-os-creds
                        key: password
                volumeMounts:
                  - name: assets
                    mountPath: /data/shared
            volumes:
              - name: assets
                persistentVolumeClaim:
                  claimName: fakecorp-prod-assets
            affinity:
              podAntiAffinity:
                requiredDuringSchedulingIgnoredDuringExecution:
                  - labelSelector:
                      matchExpressions:
                        - key: instance
                          operator: In
                          values: [fakecorp-prod]
                    topologyKey: kubernetes.io/hostname

  # ── Service selectors ───────────────────────────────────────────────────────
  - target: {kind: Service, name: dotcms}
    patch: |-
      apiVersion: v1
      kind: Service
      metadata:
        name: dotcms
      spec:
        selector:
          instance: fakecorp-prod

  - target: {kind: Service, name: dotcms-hl}
    patch: |-
      apiVersion: v1
      kind: Service
      metadata:
        name: dotcms-hl
      spec:
        selector:
          instance: fakecorp-prod

  # ── PDB selector ────────────────────────────────────────────────────────────
  - target: {kind: PodDisruptionBudget, name: dotcms}
    patch: |-
      apiVersion: policy/v1
      kind: PodDisruptionBudget
      metadata:
        name: dotcms
      spec:
        selector:
          matchLabels:
            instance: fakecorp-prod
